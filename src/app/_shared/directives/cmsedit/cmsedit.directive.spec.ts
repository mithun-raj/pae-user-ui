import { CmseditDirective } from './cmsedit.directive';

describe('CmseditDirective', () => {
  it('should create an instance', () => {
    const directive = new CmseditDirective();
    expect(directive).toBeTruthy();
  });
});
